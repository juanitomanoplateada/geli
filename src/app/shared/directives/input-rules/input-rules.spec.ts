import { InputRules } from './input-rules';

describe('InputRules', () => {
  it('should create an instance', () => {
    const directive = new InputRules();
    expect(directive).toBeTruthy();
  });
});
