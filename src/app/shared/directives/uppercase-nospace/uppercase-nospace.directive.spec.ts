import { UppercaseNospaceDirective } from './uppercase-nospace.directive';

describe('UppercaseNospaceDirective', () => {
  it('should create an instance', () => {
    const directive = new UppercaseNospaceDirective();
    expect(directive).toBeTruthy();
  });
});
